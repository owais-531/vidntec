{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "Set the Railway service Root Directory to the REPO ROOT (not apps/api) so the Docker build context includes packages/shared. Add a PostgreSQL database to this Railway project, then add the API env vars from apps/api/.env.example as service variables, setting DATABASE_URL as a reference variable: ${{Postgres.DATABASE_URL}}. The container CMD runs `prisma migrate deploy` then starts the server.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
